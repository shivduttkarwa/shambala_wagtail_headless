<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      :root {
        --nav-text: rgba(0, 0, 0, 0.7);
        --nav-text-soft: rgba(0, 0, 0, 0.55);
      }

      /* Button wrapper */
      .nav-btn {
        position: relative;
        width: 120px;
        height: 120px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        user-select: none;
        -webkit-tap-highlight-color: transparent;
        background: transparent;
        border: none;
        outline: none;
        transition: transform 0.2s ease;
      }

      /* Blobby outlines */
      .btn-outline {
        position: absolute;
        width: 120px;
        height: 120px;
        border: 1px solid rgba(0, 0, 0, 0.25);
        pointer-events: none;
        transition: border-color 0.3s ease, transform 0.3s ease;
      }

      .btn-outline-1 {
        border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
        animation: nav-morph 4s linear infinite;
      }

      .btn-outline-2 {
        border-radius: 53% 47% 43% 58% / 51% 39% 61% 49%;
      }

      @keyframes nav-morph {
        0% {
          border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
        }
        50% {
          border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%;
        }
        100% {
          border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
        }
      }

      /* Arrow container */
      .arrow-container {
        position: relative;
        width: 100%;
        height: 100%;
        z-index: 2;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      /* Base arrow */
      .arrow {
        position: relative;
        width: 34px;
        height: 2px;
        background: var(--nav-text-soft);
        border-radius: 999px;
        transition: all 0.3s ease;
      }

      .arrow::before,
      .arrow::after {
        content: "";
        position: absolute;
        width: 10px;
        height: 2px;
        background: var(--nav-text-soft);
        border-radius: 999px;
        transition: all 0.3s ease;
      }

      /* Left arrow */
      .arrow-left {
        transform-origin: center;
      }

      .arrow-left::before {
        left: 0;
        top: -4px;
        transform: rotate(40deg);
        transform-origin: left center;
      }

      .arrow-left::after {
        left: 0;
        top: 4px;
        transform: rotate(-40deg);
        transform-origin: left center;
      }

      /* Right arrow */
      .arrow-right {
        transform-origin: center;
      }

      .arrow-right::before {
        right: 0;
        top: -4px;
        transform: rotate(-40deg);
        transform-origin: right center;
      }

      .arrow-right::after {
        right: 0;
        top: 4px;
        transform: rotate(40deg);
        transform-origin: right center;
      }

      /* Hover / active states */
      .nav-btn:hover .btn-outline {
        border-color: rgba(0, 0, 0, 0.55);
        transform: scale(0.94); /* outlines shrink slightly on hover */
      }

      .nav-btn:hover .arrow,
      .nav-btn:hover .arrow::before,
      .nav-btn:hover .arrow::after {
        background: var(--nav-text);
      }

      .nav-btn:hover .arrow-right {
        transform: translateX(3px);
      }

      .nav-btn:hover .arrow-left {
        transform: translateX(-3px);
      }

      .nav-btn:active {
        transform: scale(0.95); /* whole button shrinks a bit on click */
      }
    </style>
  </head>
  <body>
    <button class="nav-btn">
      <div class="btn-outline btn-outline-1"></div>
      <div class="btn-outline btn-outline-2"></div>
      <div class="arrow-container">
        <div class="arrow arrow-left"></div>
        <!-- or arrow-right -->
      </div>
    </button>
  </body>
</html>
